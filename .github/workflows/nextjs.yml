name: Deploy Vite to GitHub Pages

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '16'  # You can adjust this to match your preferred Node version

      - name: Install dependencies
        run: |
          npm install

      - name: Build Vite site
        run: |
          npm run build  # This generates the 'dist' folder

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          branch: gh-pages  # Deploy to the 'gh-pages' branch
          folder: dist  # The output folder from 'vite build'
          token: ${{ secrets.GH_TOKEN }}  # Use your GitHub Token stored in secrets
